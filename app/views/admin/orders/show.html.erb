<%= render "/user/orders/show" %>
<section id="order-cancel">
  <% if !@order.shipped? && !@order.cancelled? %>
    <%= button_to "Cancel Order", "/admin/users/#{@order.user_id}/orders/#{@order.id}", method: :patch %>
  <% end %>
</section>