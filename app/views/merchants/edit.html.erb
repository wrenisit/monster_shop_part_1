<h1>Edit <%=link_to @merchant.name, "/merchants/#{@merchant.id}" %></h1>
<center>
  <%= render partial: 'form', locals: {path: merchant_path(@merchant), method: :patch, button_text: 'Update Merchant'} %>
</center>
