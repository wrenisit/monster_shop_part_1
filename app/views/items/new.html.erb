<h1>Create New Item For <%=link_to @merchant.name, "/merchants/#{@merchant.id}" %></h1>

<center>
  <%= render partial: 'form', locals: {path: merchant_items_path, method: :post, button_text: 'Create Item'} %>
</center>
